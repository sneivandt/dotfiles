name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  classify-changes:
    name: "CI: Classify changes"
    runs-on: ubuntu-latest
    outputs:
      docs_only: ${{ steps.classify.outputs.docs_only }}
      run_docs_checks: ${{ steps.classify.outputs.run_docs_checks }}
      run_lint: ${{ steps.classify.outputs.run_lint }}
      run_validate_config: ${{ steps.classify.outputs.run_validate_config }}
      run_rust_checks: ${{ steps.classify.outputs.run_rust_checks }}
      run_build_artifacts: ${{ steps.classify.outputs.run_build_artifacts }}
      run_profile_integration: ${{ steps.classify.outputs.run_profile_integration }}
      run_app_tests: ${{ steps.classify.outputs.run_app_tests }}
      run_git_hooks: ${{ steps.classify.outputs.run_git_hooks }}
      run_wrapper_linux: ${{ steps.classify.outputs.run_wrapper_linux }}
      run_wrapper_windows: ${{ steps.classify.outputs.run_wrapper_windows }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Determine comparison range
        id: range
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "base_sha=${{ github.event.pull_request.base.sha }}" >> "$GITHUB_OUTPUT"
            echo "head_sha=${{ github.event.pull_request.head.sha }}" >> "$GITHUB_OUTPUT"
          elif [[ "${{ github.event_name }}" == "push" && "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]]; then
            echo "base_sha=${{ github.event.before }}" >> "$GITHUB_OUTPUT"
            echo "head_sha=${{ github.sha }}" >> "$GITHUB_OUTPUT"
          else
            echo "base_sha=" >> "$GITHUB_OUTPUT"
            echo "head_sha=" >> "$GITHUB_OUTPUT"
          fi
      - name: Classify changed paths
        id: classify
        env:
          DIR: ${{ github.workspace }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ steps.range.outputs.base_sha }}
          HEAD_SHA: ${{ steps.range.outputs.head_sha }}
        run: sh .github/workflows/scripts/linux/classify-ci-changes.sh

  # ── Rust Format Check ──────────────────────────────────
  rust-fmt:
    name: "Rust: Format Check"
    runs-on: ubuntu-latest
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_rust_checks == 'true'
    defaults:
      run:
        working-directory: cli
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust toolchain
        run: rustup show
      - run: cargo fmt --check

  # ── Script Linting ────────────────────────────────────
  lint:
    name: "Lint: ${{ matrix.name }}"
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_lint == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ShellCheck
            setup: sh .github/workflows/scripts/linux/install-apt-packages.sh shellcheck
            test_function: test_shellcheck
          - name: PSScriptAnalyzer
            setup: pwsh -NoProfile -Command "Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser"
            test_function: test_psscriptanalyzer
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install ${{ matrix.name }}
        run: ${{ matrix.setup }}
      - name: Run ${{ matrix.name }}
        env:
          DIR: ${{ github.workspace }}
        run: sh .github/workflows/scripts/linux/test-static-analysis.sh ${{ matrix.test_function }}

  # ── Documentation Consistency ─────────────────────────
  docs:
    name: "Docs: Consistency"
    runs-on: ubuntu-latest
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_docs_checks == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Check documentation consistency
        env:
          DIR: ${{ github.workspace }}
        run: sh .github/workflows/scripts/linux/test-docs.sh docs_links docs_task_selectors

  # ── Configuration Validation ──────────────────────────
  validate-config:
    name: "Config: Validate all"
    runs-on: ubuntu-latest
    needs: [classify-changes, build-linux]
    if: needs.classify-changes.outputs.run_validate_config == 'true' && needs.build-linux.result == 'success'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dotfiles-linux
          path: bin/
      - name: Make binary executable
        run: chmod +x bin/dotfiles
      - name: Run typed configuration validation
        run: ./bin/dotfiles check --root . -p desktop --only config-warnings,symlink-sources,config-files
      - name: Test stock ticker
        env:
          DIR: ${{ github.workspace }}
        run: sh .github/workflows/scripts/linux/test-stocks.sh

  # ── Security ──────────────────────────────────────────
  audit:
    name: "Security: Cargo Audit"
    runs-on: ubuntu-latest
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_rust_checks == 'true'
    defaults:
      run:
        working-directory: cli
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: "cli -> target"
      - name: Install cargo-audit
        uses: taiki-e/install-action@742a3317eac7bd62f91cd888b4eead5e784ba833 # v2.87.1
        with:
          tool: cargo-audit
      - run: cargo audit

  deny:
    name: "Security: Cargo Deny"
    runs-on: ubuntu-latest
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_rust_checks == 'true'
    defaults:
      run:
        working-directory: cli
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: "cli -> target"
      - name: Install cargo-deny
        uses: taiki-e/install-action@742a3317eac7bd62f91cd888b4eead5e784ba833 # v2.87.1
        with:
          tool: cargo-deny
      - run: cargo deny check all

  # ── Build, Clippy & Test ──────────────────────────────
  build-linux:
    name: "Build: Linux"
    runs-on: ubuntu-latest
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_build_artifacts == 'true'
    defaults:
      run:
        working-directory: cli
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: "cli -> target"
          cache-on-failure: true
      - name: Build CI binary
        run: cargo build --profile ci
      - name: Clippy
        run: cargo clippy --profile ci --all-targets -- -D warnings
      - name: Tests
        run: cargo test --profile ci
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dotfiles-linux
          path: cli/target/ci/dotfiles
          retention-days: 1

  # ── MSRV (Minimum Supported Rust Version) ─────────────
  msrv:
    name: "Rust: MSRV"
    runs-on: ubuntu-latest
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_rust_checks == 'true'
    defaults:
      run:
        working-directory: cli
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Resolve MSRV
        id: msrv
        shell: bash
        run: |
          msrv="$(sed -n 's/^rust-version *= *"\([^"]*\)".*/\1/p' Cargo.toml | head -n 1)"
          if [[ -z "$msrv" ]]; then
            echo "Cargo.toml does not declare rust-version." >&2
            exit 1
          fi
          echo "version=$msrv" >> "$GITHUB_OUTPUT"
      - name: Install MSRV toolchain
        env:
          MSRV: ${{ steps.msrv.outputs.version }}
        run: rustup toolchain install "$MSRV" --profile minimal
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: "cli -> target"
          cache-on-failure: true
      - name: Check against MSRV
        env:
          MSRV: ${{ steps.msrv.outputs.version }}
        run: cargo "+$MSRV" check --all-targets

  # ── Coverage (informational, non-gating) ──────────────
  coverage:
    name: "Rust: Coverage (${{ matrix.name }})"
    runs-on: ${{ matrix.os }}
    continue-on-error: true
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_rust_checks == 'true'
    defaults:
      run:
        working-directory: cli
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-latest
          - name: Windows
            os: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: "cli -> target"
          cache-on-failure: true
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@742a3317eac7bd62f91cd888b4eead5e784ba833 # v2.87.1
        with:
          tool: cargo-llvm-cov
      - name: Generate coverage report
        run: cargo llvm-cov --profile ci --all-targets --summary-only
      - name: Generate HTML coverage report
        run: cargo llvm-cov report --profile ci --html
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: rust-coverage-${{ matrix.name }}
          path: cli/target/llvm-cov/html
          if-no-files-found: ignore
          retention-days: 7

  # ── Mutation Testing (informational, non-gating) ───────
  mutation:
    name: "Rust: Mutation (changed code)"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    continue-on-error: true
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_rust_checks == 'true' && github.event_name == 'pull_request'
    defaults:
      run:
        working-directory: cli
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: "cli -> target"
          cache-on-failure: true
      - name: Install cargo-mutants
        uses: taiki-e/install-action@742a3317eac7bd62f91cd888b4eead5e784ba833 # v2.87.1
        with:
          tool: cargo-mutants
      - name: Build changed-code diff
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: git diff --relative=cli --unified=0 "$BASE_SHA..$HEAD_SHA" -- src > mutants.diff
      - name: Test changed-code mutants
        run: cargo mutants --in-place --no-shuffle --in-diff mutants.diff
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: rust-mutation-results
          path: cli/mutants.out
          if-no-files-found: ignore
          retention-days: 7

  build-windows:
    name: "Build: Windows"
    runs-on: windows-latest
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_build_artifacts == 'true'
    defaults:
      run:
        working-directory: cli
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Disable Windows Defender real-time monitoring
        continue-on-error: true
        run: |
          try {
            Set-MpPreference -DisableRealtimeMonitoring $true -ErrorAction Stop
          } catch {
            Write-Host "Could not disable Defender real-time monitoring (likely tamper protection); continuing: $_"
          }
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: "cli -> target"
          cache-on-failure: true
      - name: Build CI binary
        run: cargo build --profile ci
      - name: Clippy
        run: cargo clippy --profile ci --all-targets -- -D warnings
      - name: Tests
        run: cargo test --profile ci
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dotfiles-windows
          path: cli/target/ci/dotfiles.exe
          retention-days: 1

  # ── Integration Tests ─────────────────────────────────
  integration-linux:
    name: "Integration: ${{ matrix.profile }} on Linux"
    runs-on: ubuntu-latest
    needs: [classify-changes, build-linux]
    if: needs.classify-changes.outputs.run_profile_integration == 'true'
    strategy:
      fail-fast: false
      matrix:
        profile: [base, desktop]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dotfiles-linux
          path: bin/
      - name: Make binary executable
        run: chmod +x bin/dotfiles
      - name: Dry-run install
        run: ./bin/dotfiles install --root . -p ${{ matrix.profile }} -n --skip vscode-extensions
      - name: Run validation
        run: ./bin/dotfiles check --root . -p ${{ matrix.profile }}

  integration-windows:
    name: "Integration: ${{ matrix.profile }} on Windows"
    runs-on: windows-latest
    needs: [classify-changes, build-windows]
    if: needs.classify-changes.outputs.run_profile_integration == 'true'
    strategy:
      fail-fast: false
      matrix:
        profile: [base, desktop]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dotfiles-windows
          path: bin/
      - name: Dry-run install
        shell: bash
        run: ./bin/dotfiles.exe install --root . -p ${{ matrix.profile }} -n --skip vscode-extensions
      - name: Run validation
        shell: bash
        run: ./bin/dotfiles.exe check --root . -p ${{ matrix.profile }}

  test-install-uninstall:
    name: "Integration: Install/Uninstall round-trip"
    runs-on: ubuntu-latest
    needs: [classify-changes, build-linux]
    if: needs.classify-changes.outputs.run_profile_integration == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dotfiles-linux
          path: bin/
      - name: Make binary executable
        run: chmod +x bin/dotfiles
      - name: Run install/uninstall round-trip test
        env:
          DIR: ${{ github.workspace }}
          BINARY_PATH: ${{ github.workspace }}/bin/dotfiles
        run: sh .github/workflows/scripts/linux/test-uninstall.sh

  test-install-uninstall-windows:
    name: "Integration: Install/Uninstall round-trip (Windows)"
    runs-on: windows-latest
    needs: [classify-changes, build-windows]
    if: needs.classify-changes.outputs.run_profile_integration == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dotfiles-windows
          path: bin/
      - name: Run install/uninstall round-trip test
        shell: pwsh
        env:
          DIR: ${{ github.workspace }}
          BINARY_PATH: ${{ github.workspace }}/bin/dotfiles.exe
        run: ./.github/workflows/scripts/windows/Test-InstallUninstall.ps1

  # ── Application Tests ─────────────────────────────────
  test-applications:
    name: "App: ${{ matrix.application }}"
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [classify-changes, build-linux]
    if: needs.classify-changes.outputs.run_app_tests == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - application: git
            packages: git
            tests: config aliases behavior
          - application: zsh
            packages: zsh
            tests: completion
          - application: vim
            packages: vim
            tests: opens
          - application: nvim
            packages: neovim
            tests: opens plugins
          - application: volume
            packages: coreutils
            tests: init
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install ${{ matrix.packages }}
        run: sh .github/workflows/scripts/linux/install-apt-packages.sh ${{ matrix.packages }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dotfiles-linux
          path: bin/
      - name: Make binary executable
        run: chmod +x bin/dotfiles
      - name: Install dotfiles with base profile
        run: ./bin/dotfiles install --root . -p base -v --skip apm
      - name: Test ${{ matrix.application }}
        env:
          DIR: ${{ github.workspace }}
        run: sh .github/workflows/scripts/linux/test-applications.sh ${{ matrix.application }} ${{ matrix.tests }}

  # Windows counterpart. Only git is covered: zsh, vim, and nvim are excluded
  # from the Windows profile, so they have no Windows equivalent to test.
  test-applications-windows:
    name: "App: ${{ matrix.application }} (Windows)"
    runs-on: windows-latest
    needs: [classify-changes, build-windows]
    if: needs.classify-changes.outputs.run_app_tests == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - application: Git
            tests: Config Alias Behavior
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dotfiles-windows
          path: bin/
      - name: Install dotfiles with base profile
        shell: pwsh
        # Skip unavailable external installers: winget installs fail inside CI
        # and windows-latest does not provide a VS Code CLI launcher. This job
        # validates the git symlink and config chain instead.
        run: .\bin\dotfiles.exe install --root . -p base -v --skip apm,packages,vscode-extensions
      - name: Test ${{ matrix.application }}
        shell: pwsh
        env:
          DIR: ${{ github.workspace }}
        run: ./.github/workflows/scripts/windows/Test-Applications.ps1 ${{ matrix.application }} ${{ matrix.tests }}

  # ── Specialized Tests ─────────────────────────────────
  test-git-hooks:
    name: "Security: Pre-commit Sensitive Data Detection"
    runs-on: ubuntu-latest
    needs: [classify-changes]
    if: needs.classify-changes.outputs.run_git_hooks == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install and test pre-commit hook
        run: |
          git config user.name "CI Test"
          git config user.email "ci@test.local"
          ln -sf "$(pwd)/hooks/pre-commit" .git/hooks/pre-commit
          chmod +x .git/hooks/pre-commit
          .github/workflows/scripts/linux/test-git-hooks.sh

  test-shell-wrapper-linux:
    name: "Shell Wrapper: Linux (dotfiles.sh)"
    runs-on: ubuntu-latest
    needs: [classify-changes, build-linux]
    if: needs.classify-changes.outputs.run_wrapper_linux == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dotfiles-linux
          path: bin/
      - name: Make binary executable
        run: chmod +x bin/dotfiles
      - name: Run shell wrapper tests
        env:
          DIR: ${{ github.workspace }}
          BINARY_PATH: ${{ github.workspace }}/bin/dotfiles
        run: sh .github/workflows/scripts/linux/test-shell-wrapper.sh

  test-shell-wrapper-windows:
    name: "Shell Wrapper: Windows (dotfiles.ps1)"
    runs-on: windows-latest
    needs: [classify-changes, build-windows]
    if: needs.classify-changes.outputs.run_wrapper_windows == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dotfiles-windows
          path: bin/
      - name: Run PowerShell wrapper tests
        shell: pwsh
        env:
          BINARY_PATH: ${{ github.workspace }}/bin/dotfiles.exe
        run: ./.github/workflows/scripts/windows/Test-ShellWrapper.ps1

  # ── CI Gate ───────────────────────────────────────────
  ci-success:
    name: "All CI Checks Passed"
    runs-on: ubuntu-latest
    if: always()
    needs:
      - classify-changes
      - rust-fmt
      - lint
      - docs
      - validate-config
      - audit
      - deny
      - build-linux
      - msrv
      - build-windows
      - integration-linux
      - integration-windows
      - test-install-uninstall
      - test-install-uninstall-windows
      - test-applications
      - test-applications-windows
      - test-git-hooks
      - test-shell-wrapper-linux
      - test-shell-wrapper-windows
    steps:
      - name: Check all jobs passed
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" || "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "One or more CI jobs failed or were cancelled"
            exit 1
          fi
          echo "All required CI jobs passed or were intentionally skipped"
