[package]
name = "dotfiles-cli"
# Not the release version. Releases are tagged `vYYYY.MM.DD.N` by
# .github/workflows/release.yml and injected via DOTFILES_VERSION; this value
# only backs the `dev-` fallback reported by local builds.
version = "0.1.0"
edition = "2024"
rust-version = "1.91"
description = "Cross-platform dotfiles management engine"
license = "MIT"
publish = false

[[bin]]
name = "dotfiles"
path = "src/main.rs"

[dependencies]
anyhow = "1"
base64 = "0.23"
clap = { version = "4", features = ["derive"] }
clap_complete = "4"
ctrlc = "3.5"
dunce = "1"
thiserror = "2"
enable-ansi-support = "0.3"
git2 = "0.21.0"
rayon = "1.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
toml = "1"
tracing = "0.1"
ureq = "3"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
which = "8.0.3"
terminal_size = "0.4.4"
serde_yaml_ng = "0.10.0"
unicode-width = "0.2"

[dev-dependencies]
insta = { version = "1", features = ["yaml"] }
mockall = "0.15"
tempfile = "3"
# tests/domain_boundaries.rs parses src/ as a syntax tree instead of scanning it
# as text. `span-locations` is what makes line numbers available outside a
# proc-macro context, so violations can still be reported as `file:line`.
#
# Known cost: `syn`'s `visit` feature and `proc-macro2`'s `span-locations` also
# apply to the host copies of those crates that every derive macro in the tree
# builds against, because Cargo unifies host and target features when not
# cross-compiling. `cargo build` excludes dev-dependencies and `cargo test`
# includes them, so the two commands resolve different feature sets and evict
# each other's cached build of this crate (~15-30s per switch). Repeating
# either command on its own is unaffected. Narrowing the features does not
# help: `visit` is the feature the test exists to use. Moving the test into a
# separate package is the only real fix.
syn = { version = "2", features = ["full", "visit"] }
proc-macro2 = { version = "1", features = ["span-locations"] }

[target.'cfg(windows)'.dependencies]
winreg = "0.56"

[target.'cfg(unix)'.dependencies]
nix = { version = "0.31", features = ["process", "signal", "user"] }

[lints.clippy]
pedantic = { level = "deny", priority = -1 }
nursery = { level = "deny", priority = -1 }
cargo = { level = "deny", priority = -1 }
multiple_crate_versions = "allow"
# `unreachable_pub` (rust lint) and `redundant_pub_crate` give contradictory
# advice for `pub(crate) mod foo; pub fn bar` patterns. We prefer
# `pub(crate)` everywhere internal so consumers can see the true API surface.
redundant_pub_crate = "allow"
# Restriction lints (opt-in individually)
allow_attributes_without_reason = "deny"
arithmetic_side_effects = "deny"
as_conversions = "deny"
clone_on_ref_ptr = "deny"
dbg_macro = "deny"
empty_structs_with_brackets = "deny"
expect_used = "deny"
fn_to_numeric_cast_any = "deny"
get_unwrap = "deny"
if_then_some_else_none = "deny"
indexing_slicing = "deny"
infinite_loop = "deny"
let_underscore_must_use = "deny"
lossy_float_literal = "deny"
missing_assert_message = "deny"
panic = "deny"
print_stderr = "deny"
print_stdout = "deny"
redundant_type_annotations = "deny"
rest_pat_in_fully_bound_structs = "deny"
shadow_unrelated = "deny"
todo = "deny"
undocumented_unsafe_blocks = "deny"
unimplemented = "deny"
unnecessary_self_imports = "deny"
unreachable = "deny"
unused_result_ok = "deny"
unwrap_used = "deny"
verbose_file_reads = "deny"
wildcard_enum_match_arm = "deny"

[lints.rust]
dead_code = "deny"
explicit_outlives_requirements = "deny"
future_incompatible = { level = "deny", priority = -1 }
unsafe_code = "deny"
let_underscore_drop = "deny"
macro_use_extern_crate = "deny"
missing_debug_implementations = "deny"
missing_docs = "deny"
non_ascii_idents = "deny"
trivial_numeric_casts = "deny"
unreachable_pub = "deny"
unused_extern_crates = "deny"
unused_lifetimes = "deny"
unused_qualifications = "deny"

[lints.rustdoc]
broken_intra_doc_links = "deny"
invalid_codeblock_attributes = "deny"
invalid_html_tags = "deny"
bare_urls = "deny"
unescaped_backticks = "deny"

[profile.dev.package."*"]
opt-level = 3

[profile.dev-opt]
inherits = "dev"
opt-level = 1
incremental = false
codegen-units = 256
debug = 0

[profile.ci]
inherits = "dev"
opt-level = 1
lto = false
codegen-units = 256
debug = 0

[profile.release]
strip = true
lto = true
codegen-units = 1
opt-level = "z"
