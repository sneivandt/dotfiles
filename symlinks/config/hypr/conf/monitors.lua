-- Monitor — use preferred resolution, auto-placement
hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = 1,
    reserved_area = { top = 30, bottom = 0, left = 0, right = 0 },
})
