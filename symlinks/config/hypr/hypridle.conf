# Hypridle — idle management for Hyprland

general {
    lock_cmd = ~/.config/hypr/scripts/lock-screen.sh
    before_sleep_cmd = ~/.config/hypr/scripts/lock-screen.sh
    after_sleep_cmd = hyprctl dispatch dpms on
}

# Lock after 10 minutes
listener {
    timeout = 600
    on-timeout = ~/.config/hypr/scripts/lock-screen.sh
}

# Turn off display after 15 minutes, unless the session is locked.
listener {
    timeout = 900
    on-timeout = pidof hyprlock >/dev/null || hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}
