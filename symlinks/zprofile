#!/usr/bin/env zsh

# Guard against multiple sourcing
[ -n "$ZPROFILE_LOADED" ] && return
ZPROFILE_LOADED=1

# Start X Windows on tty1 if not already running
[ -z "$DISPLAY" ] && [ "$XDG_VTNR" -eq 1 ] && exec startx
